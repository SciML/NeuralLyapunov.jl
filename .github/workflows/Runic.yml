name: Runic Formatting

on:
  push:
    branches:
      - master
    tags: ['*']
  pull_request:
  workflow_dispatch:

jobs:
  runic:
    name: Runic Formatting Check
    uses: "SciML/.github/.github/workflows/runic.yml@v1"
    secrets: "inherit"
